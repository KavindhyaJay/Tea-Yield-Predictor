# Leave empty to run the frontend on mock data (default).
# Point it at the FastAPI service to use the trained CatBoost model.
# VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=https://tea-yield-predictor.onrender.com/
